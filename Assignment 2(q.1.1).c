#include<stdio.h>
int main()
{
	 int l,b,area;
	 printf("enter length and breadth");
	 scanf("%d%d",&l,&b);
	 //area=l*b;
	 printf("length is %d and breadth is %d",l,b);
	 printf("your area is%d",area);
	 return 0;
	 
}
